package com.example.springframework.Autowiring;

public class Intro {
    /*
    -feature which spring container inject the dependencies automatically
    -it is only used for object not for primitive and string type
    ########Ways
   1. XML
        -no
        -byName
        -byType
        -constructor
        -autodetect
   2. Annotation
        -@Autowired
     */
}

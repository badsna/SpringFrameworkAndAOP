package com.example.springframework.ForAspect;

public class ProxyIntro {
    /*
    how the class A calls the method of class B
    proxy design patter:
        -Instead of making call to class A we call class B which internally call class A with additional functionality
     FactoryService to get bean of its own

     */
}
